version: '2.0'

services:
  builder:
    image: app-prod
    build:
      dockerfile: Dockerfile-build
      context: .
  tester:
    depends_on:
      - builder
    build:
      dockerfile: Dockerfile-test
      context: .
