version: '2.0'

services:
  builder:
    image: generalcube/mm301180_dev_ops:rc01
    build:
      dockerfile: Dockerfile-build
      context: .
  tester:
    depends_on:
      - builder
    build:
      dockerfile: Dockerfile-test
      context: .
